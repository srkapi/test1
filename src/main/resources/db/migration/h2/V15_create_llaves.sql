CREATE TABLE LLAVES (
      CODE BIGINT GENERATED BY DEFAULT AS IDENTITY,
      NAME VARCHAR(255),
      ID_PERSONAJE BIGINT,
      PRIMARY KEY (CODE)
);

ALTER TABLE LLAVES ADD CONSTRAINT ID FOREIGN KEY (ID_PERSONAJE) REFERENCES PERSONAJES;